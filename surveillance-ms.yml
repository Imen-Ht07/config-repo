spring:
  datasource:
    url: jdbc:postgresql://surveillance-db:5432/surveillance
    username: surveillance
    password: surveillance
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  kafka:
    bootstrap-servers: kafka.anomalies.svc.cluster.local:9092
    topic:
      anomalies: anomalies.new
  config:
    import: optional:configserver:http://config-server:8888

eureka:
  client:
    serviceUrl:
      defaultZone: http://eureka:8761/eureka/

management:
  endpoints:
    web:
      exposure:
        include: health,info
